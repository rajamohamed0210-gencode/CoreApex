# Core Apex.dev Environment Configuration
DEBUG=True
SECRET_KEY=core-apex-dev-super-secure-production-key-2026-xyz!#9928
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database Configuration (PostgreSQL 18)
# If DB_NAME is left as sqlite or PostgreSQL credentials are not connected, SQLite will be used as zero-config local fallback.
DB_ENGINE=django.db.backends.postgresql
DB_NAME=coreapex_db
DB_USER=postgres
DB_PASSWORD=your_secure_postgres18_password
DB_HOST=localhost
DB_PORT=5432

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000
