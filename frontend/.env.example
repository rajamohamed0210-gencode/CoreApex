# Frontend environment configuration
#
# Leave VITE_API_URL unset for local development: requests go to the same origin
# at /api/v1 and Vite proxies them to the Django backend (see vite.config.js).
#
# Set it when the API is hosted elsewhere, e.g.
# VITE_API_URL=https://api.coreapex.dev/api/v1
